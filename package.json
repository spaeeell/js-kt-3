{
  "scripts": {
    "test": "vitest watch --reporter=dot",
    "test:report": "vitest run",
    "lint": "oxlint src --fix"
  },
  "devDependencies": {
    "oxlint": "^1.23.0",
    "vitest": "^3.2.4"
  }
}